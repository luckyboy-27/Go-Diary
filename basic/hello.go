package main

import (
	"fmt"
	"math"
	"math/rand"
)

func main() {
	fmt.Println("Hello, World!")
	fmt.Println(math.Pi)
	fmt.Println("A random number:", rand.Intn(100))
	//Data type in Go --> we can not change the data types like PHP or JS
	//Data type: Bool, int8, int16, int32, int64, int uint8, uint16, uint32, uint64, uint float32, float64 complex64, complex128 byte rune, string
	//How to call a variable?
}
